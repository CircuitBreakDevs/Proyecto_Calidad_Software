# ─────────────────────────────────────────────────────────────────
# Dependencias del Proyecto: Picas y Fijas
# ─────────────────────────────────────────────────────────────────

# --- Servidor y API ---
fastapi==0.111.0        # Framework web para construir la API REST
uvicorn==0.29.0         # Servidor ASGI para ejecutar FastAPI
jinja2==3.1.4           # Motor de plantillas para el HTML
httpx==0.27.0           # Cliente HTTP (usado por TestClient de FastAPI)

# --- Pruebas ---
pytest==8.2.2           # Framework principal de pruebas
pytest-bdd==7.2.0       # Extensión para pruebas BDD con Gherkin
pytest-cov==5.0.0       # Plugin para medir cobertura de código
pytest-asyncio==0.23.7  # Soporte para pruebas asíncronas con pytest
anyio==4.4.0            # Backend de concurrencia para pruebas

# --- Pruebas E2E ---
playwright==1.44.0      # Automatización del navegador para pruebas E2E

# --- Análisis Estático ---
flake8==7.0.0           # Linter: detecta errores de estilo y código muerto
